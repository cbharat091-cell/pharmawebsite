{
  "version": 2,
  "functions": {
    "final_pro/wsgi.py": {
      "runtime": "python3.9",
      "maxLambdaSize": "15mb"
    }
  },
  "routes": [
    {
      "src": "/(.*)",
      "dest": "final_pro/wsgi.py"
    }
  ],
  "installCommand": "pip install -r requirements.txt",
  "buildCommand": "python manage.py collectstatic --noinput && python manage.py migrate --noinput",
  "env": {
    "DJANGO_SETTINGS_MODULE": "final_pro.settings"
  }
}
